interface:
  display_name: "Agents"
  short_description: "Operate visible Claude, opencode, and Cursor sessions."
  default_prompt: "Use $agents to discover, start, resume, fork, attach, or archive top-level visible Claude, opencode, or Cursor sessions. Use Codex Desktop thread tools for Codex threads."
