#!/usr/bin/env bash
set -Eeuo pipefail

cockpit_cli=/mnt/server-ssd/BJsWorkspace/cockpit/bin/cockpit

[[ -x "$cockpit_cli" ]] || { printf 'Cockpit CLI unavailable: %s\n' "$cockpit_cli" >&2; exit 2; }

exec "$cockpit_cli" linear "$@"
